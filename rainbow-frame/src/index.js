import React from 'react';
import ReactDOM from 'react-dom/client';
import RainbowFrameApp from './RainbowFrameApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RainbowFrameApp />
  </React.StrictMode>
);